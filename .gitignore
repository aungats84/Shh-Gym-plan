node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store
coverage
screenshots
design-screenshots
.env.check

# TypeScript build caches - regenerated on every build, never worth committing.
*.tsbuildinfo

# Impeccable tooling: keep config.json, ignore the transient cache/build state.
.impeccable/build/
.impeccable/hook.cache.json
